# Composition-Harmonizer
Build a browser-based compositing tool: Input: background image + foreground PNG with transparency User action: drag/scale/rotate foreground onto background Output: integrated composite via local matching + edge feather + export PNG No backend.
